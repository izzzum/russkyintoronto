
import { formatEnding } from 'my-app/helpers/format-ending';
import { module, test } from 'qunit';

module('Unit | Helper | format ending');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatEnding([42]);
  assert.ok(result);
});

