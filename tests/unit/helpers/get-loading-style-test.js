
import { getLoadingStyle } from 'my-app/helpers/get-loading-style';
import { module, test } from 'qunit';

module('Unit | Helper | get loading style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getLoadingStyle([42]);
  assert.ok(result);
});

