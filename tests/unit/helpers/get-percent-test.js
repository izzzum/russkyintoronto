
import { getPercent } from 'my-app/helpers/get-percent';
import { module, test } from 'qunit';

module('Unit | Helper | get percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPercent([42]);
  assert.ok(result);
});

