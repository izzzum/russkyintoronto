
import { replyToLink } from 'my-app/helpers/reply-to-link';
import { module, test } from 'qunit';

module('Unit | Helper | reply to link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = replyToLink([42]);
  assert.ok(result);
});

