
import { textToLink } from 'my-app/helpers/text-to-link';
import { module, test } from 'qunit';

module('Unit | Helper | text to link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = textToLink([42]);
  assert.ok(result);
});

