import Ember from 'ember';

export default Ember.Component.extend({
    comment: null
    //id: null,
    //store: Ember.inject.service(),
    //comment: Ember.computed(function() {return this.get('store').peekRecord('comment', this.get('id'));})
});
