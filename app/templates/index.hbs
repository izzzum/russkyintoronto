<nav class="menu">
    <a class="getStats" {{action 'getStats'}}>
        <i class="fa fa-line-chart"></i> 
        {{#unless displayStats}}Показать статистику{{/unless}}
        {{#if displayStats}}Убрать статистику{{/if}}
    </a>
    <a class="scrollTop" {{action 'scrollTop'}}>
        <i class="fa fa-arrow-up"></i> Вверх
    </a>
    <a class="scrollBot" {{action 'scrollBot'}}>
        <i class="fa fa-arrow-down"></i> Вниз
    </a>
</nav>
{{#if showLoader}}
{{loader-img show=showLoader}}
{{/if}}
{{#if displayStats}}
{{put-stats list=list}}
{{/if}}
{{posts-container posts=model.posts count=count loadedPortion=loadedPortion}}