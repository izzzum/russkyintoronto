<div class="container">
    <h3>Для статистики проанализировано {{list.postsNum}} {{format-ending num=user.items word='post'}}</h3>
        <ul>
            <li>Написано комментариев: {{list.commentsNum}}</li>
            <li>Количество активных пользователей: {{list.userNum}}</li>
        </ul>
    <h4>Топ-{{array-length list.topCommentsUsers}} {{format-ending num=(array-length list.topCommentsUsers) word='user'}} по оставленным комментариям:</h4>
    <div class="box">
        <ol>
        {{#each list.topCommentsUsers as |user|}}
        <li>
            <a href="http://vk.com/id{{user.user.id}}"><img class="img-circle" src="{{user.user.photo50}}"></a>
            <a class="name" href="http://vk.com/id{{user.user.id}}">{{get-fullname user.user.firstName user.user.lastName}}</a>
            {{percent-bar rate=(get-percent total=list.commentsNum fraction=user.items)}}
            <p>{{#if (eq user.user.sex '1')}}
                оставила
            {{else}}
                оставил
            {{/if}}
            {{user.items}} {{format-ending num=user.items word='comment'}}</p>
            
        </li>
        {{/each}}
        </ol>
    </div>
    <h4>Топ-{{array-length list.topPostsUsers}} {{format-ending num=(array-length list.topPostsUsers) word='user'}} по написанным постам:</h4>
    <div class="box">
        <ol>
        {{#each list.topPostsUsers as |user|}}
        <li>
            <a href="http://vk.com/id{{user.user.id}}"><img class="img-circle" src="{{user.user.photo50}}"></a>
            <a class="name" href="http://vk.com/id{{user.user.id}}">{{get-fullname user.user.firstName user.user.lastName}}</a>
            {{#if (eq user.user.sex '1')}}
                написала
            {{else}}
                написал
            {{/if}}
            {{user.items}} {{format-ending num=user.items word='post'}}
        </li>
        {{/each}}
        </ol>
    </div>
    <h4>Топ-{{array-length list.topCommentedPosts}} {{format-ending num=(array-length list.topCommentedPosts) word='post'}} по обсуждаемости:</h4>
    <div class="box">
        <ol>
        {{#each list.topCommentedPosts as |post|}}
        <li>
          Опубликован: <span class="time">{{get-date post.post.date}}</span><br>
          {{#link-to 'post' post.post.id}}<i class="fa fa-link"> Ссылка на пост</i>{{/link-to}}
          {{single-post post=post.post simple=true}}
        </li>
        {{/each}}
        </ol>
    </div>
    <h4>Топ-{{array-length list.mostLikedComments}} {{format-ending num=(array-length list.mostLikedComments) word='comment'}} по количеству лайков:</h4>
    <div class="box">
        <ol>
        {{#each list.mostLikedComments as |comment|}}
        <li>
            Опубликован: <span class="time">{{get-date comment.comment.post.date}}</span><br>
            {{#link-to 'post' comment.comment.post.id}}<i class="fa fa-link"> Ссылка на пост</i>{{/link-to}}
            {{single-comment comment=comment.comment simple=true}}
        </li>
        {{/each}}
        </ol>
    </div>
</div>